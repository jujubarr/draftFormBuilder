{
  "name": "protractorserver",
  "version": "1.0.0",
  "description": "Runs protractor test case via API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Rosemary",
  "license": "ISC",
  "dependencies": {
    "chromedriver": "^2.24.1",
    "express": "^4.14.0"
  },
  "devDependencies": {
    "webdriver-manager": "^10.2.3"
  }
}
